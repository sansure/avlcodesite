name = 'stats-tracker'
main = "src/index.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
# 使用前请先执行: wrangler d1 create stats-db
# 然后将返回的 database_id 填入下方
[[d1_databases]]
binding = "DB"
database_name = "stats-db"
database_id = "REPLACE_WITH_YOUR_DATABASE_ID"
